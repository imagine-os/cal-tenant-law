# 0017 - Game board verified against the 2021 poster, and the board pages fixed

version: 0.2.0
date: 2026-09-20
prompt: 0006
intent: Justin: "Double check the Gameboard, and make sure it's all correct and working proper." Verify docs/game-board/nodes.json square by square and arrow by arrow against reference/game-board.pdf, fix what the poster clearly contradicts, record what only the firm's attorney can settle, then verify GB-01, GB-02 and GB-03 actually render and behave at every width, in all three brands, light and dark.
decision: Read the poster twice over. (1) Text: the PDF's text layer extracted with per-run coordinates (pypdf visitor, the title font's /gidNNNNN glyph names decoded) gives all 251 printed strings; matched against the data, every square is present, none is missing and no label differs, so nothing was retired or renamed. (2) Artwork: the page rendered with PDFium at 2.2x and read region by region, because the poster colour-codes its arrows with its own KEY - so an arrow's path type is a fact, not a reading. That settled 23 wrong path types, five wrong endpoints and one missing edge (114 -> 115 paths), cleared nine reconstructed edges (verified_against: "poster image 2026-09-20"), and honestly marked two more as reconstructed where the data was more confident than the poster. Thirteen edges remain reconstructed: they are listed one by one in docs/game-board/verification-2026-09-20.md with what the poster does and does not show, for the attorney. In the app: the type band now follows the viewport rather than the board's container (the shell eats ~384 px, so a 1920 screen was painting 14 px labels) and every string the SVG draws is floored at labelFont - 1, giving 16 px at 1920 and 17 px at 3840; the minimap moved to the bottom-left, out from under the shell's floating Feedback button; the toolbar d-pad got the action id it was missing (board.pan, four directions, also on BoardCommand so a remote or the voice controller drives the same view, P-05/P-04); GB-02 gained its own empty state for a case id with no board_positions row and falls back to the Select atom above five cases (the seed now has 24, which overflowed the SegmentedControl in the page header); and the detail drawer stopped showing a "cost ?" placeholder over the 45 squares whose cost band the services catalog has already filled from the store SKUs.
rejected: Changing the node labels "Granted: Return to prior status" and "Denied: You Appeal" to drop the colon the poster does not print (the poster underlines the first word as a heading line; the colon preserves that reading, and it is recorded as a choice, not a typo). Splitting the removal phase into the poster's two printed headings, "Removal to Federal Court" and "Petition" (it changes every node's phase in that region and is Justin's call, not a transcription fix). Re-colouring the squares by phase the way the poster does (it is the better match, but it is a restyle and the formatting pass owns it; the side effect is noted - "Evaluate Service: Good or Bad" now paints as a negative square because a red arrow reaches it). Deleting the reconstructed edges with no arrow on the poster (they carry real procedure; marked, not removed). Rewriting toneOf, the edge router or anything else cosmetic. Guessing any deadline: every deadline stays a marked placeholder until T-059 cites a statute row.
files: docs/game-board/nodes.json, docs/game-board/README.md, docs/game-board/verification-2026-09-20.md, src/components/organism/GameBoard/GameBoard.tsx, src/components/organism/GameBoard/GameBoard.css, src/components/organism/GameBoard/GameBoard.meta.ts, src/components/organism/GameBoard/layout.ts, src/components/organism/GameBoard/types.ts, src/modules/board/BoardPage.tsx, src/modules/board/CaseBoardPage.tsx, src/modules/board/OverlayPage.tsx, src/modules/board/NodeDetail.tsx, src/modules/board/boardData.ts, src/modules/board/specs.ts, src/modules/board/strings.ts, docs/pages/GB-01.md, docs/pages/GB-02.md, docs/pages/GB-03.md, docs/changelog/_pending/board-verify.md
codes: GB-01 GB-02 GB-03

Merged from `_pending/board-verify.md` at release 0.2.0 (changelog 0029).

Model: Opus 5 (1M context).

## Data corrections (docs/game-board/nodes.json)

- **Path types, from the KEY's colours** (23): the four ways into `default-entered-by-clerk` are black Neutral, not red Negative; `discovery-requests` reaches both `good-responses` and `no-response-or-mostly-objections` on the tan Normal band, not on Positive / Negative; `evaluate-service` leaves red to "Service Bad" and green to "Evaluate Complaint", which is the reverse of what the data said; `good-responses -> compile-information-for-trial-preparation` is the grey Jump arc round the right edge; `judgment-entered-writ-issued`, `five-day-notice-to-vacate`, `you-lose -> notice-of-appeal`, `motion-to-quash-denied -> petition-for-writ-of-mandate`, `landlord-files-first-amended-complaint`, `evaluate-complaint-for-demurrer -> answer-to-complaint` and `new-service-new-motion` are all black Neutral; `ex-parte-hearing -> demurrer-hearing`, `prepare-jury-trial-papers -> continuance-transfer-for-jury-trial` and the two service-band edges are tan Normal; `sustained -> landlord-files-first-amended-complaint` is the green line running up out of Sustained; `writ-of-mandate-decision -> petition-for-transfer-to-district-court` and `removal-denied-remanded` are red Negative.
- **Endpoints** (5): Motion to Quash Granted's black arrow runs to **Process Server tries to serve you**, not to New Service, New Motion; the arrow into New Service, New Motion leaves **Taken off calendar; reset hearing** (added); the tan band into Ex Parte App to shorten time drops out of **Opposition to Demurrer**, not Demurrer; summary judgment's Motion Denied points at **Jury Trial Requested**; the appeals-court stay request's red run ends at **Sheriff returns to perform lockout. You Move**.
- **meta**: the poster's two printed taglines and its footer paragraph are now data (`meta.taglines`, `meta.footer`), plus `verified_on` / `verified_by`.

## App fixes

| Page | Fix |
| --- | --- |
| GB-01 / organism | `layoutOptionsFor(containerWidth, viewportWidth)`: columns from the container, type band from the viewport (15 / 17 / 18); `floorFont = labelFont - 1` applied to the overlay badge, edge labels, "YOU ARE HERE" and the phase step. Measured 16 px at 1920, 17 px at 3840 |
| GB-01 / organism | minimap moved from bottom-right to bottom-left (the staff shell's Feedback button owns the bottom-right corner) |
| GB-01 / 02 / 03 | `board.pan { direction: up\|down\|left\|right }` added to the shared board actions and wired on all three pages; `BoardCommand` kinds extended |
| GB-02 | unknown case id gets `board.case.missing` / `board.case.missingBody` instead of the "no demo cases" wording |
| GB-02 | case selector falls back to `Select` above five cases so the page header never overflows |
| GB-03 + drawer | `NodeDetail` prints a real `typical_cost_band` and only placeholders a null one; `BoardPage` and `OverlayPage` pass the square with `board_node_meta` merged |
| GB-03 | spec logic, `board.overlay.subtitle`, `explainTitle` and `explain` no longer claim every cost band is empty |

## Checked

Playwright, Chromium from `/opt/pw-browsers`, dev server at 5199. GB-01 at 360 / 390 / 768 / 1280 / 1920 / 3840: 88 squares, 115 paths, phases in order, no horizontal page overflow, **0 console errors**. KEY swatch = painted stroke = arrowhead fill for all five path types in clearsky / boardgame / courthouse, light and dark. Square-label contrast 4.69:1 or better everywhere (WCAG AA); path colour against the board surface 4.97:1 or better. Path filters, zoom buttons, fit, reset, `?node=` deep link (and an unknown one), Tab order and focus-reveal all behave. GB-02: `/board/case` redirect, selector, current square, visited path, next moves, missing case. GB-03: 88 badges, 45 real bands, 43 + every deadline as `data-placeholder`. `npm run typecheck` clean for every file in this change.
