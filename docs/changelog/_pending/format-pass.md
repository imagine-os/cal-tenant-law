# format-pass - Pass 2 wave A: spacing, hierarchy and usability pass across every shell and page

version: 0.2.0-dev
date: 2026-09-20
prompt: 0006
intent: Justin's first line ("a deep pass to make sure that all the spacing and formatting looks good. Try to make things more user friendly") and, from the pass-one thread, "focus on content" and "make it easy to switch between the 3 styles". Not a redesign: one rhythm, one hierarchy and one set of component rules enforced on all three skins and both themes, from a before / after capture of every route at 390, 1280, 1920 and 3840 as the surface's demo role.
decision: The shell owns the page padding and the section gap (`.shell-content > *` is one column with `--sp-6` gaps; `.page` is neutral inside the shell; boards / consoles / canvas opt out of the centred width with `.page-bleed`); a framed table never repeats its Section's title; dense table rows are 44 px; row actions never wrap; long text columns opt into `wrap`; PageHeader actions are a two-column grid on phones; toasts come from the top on phones; the sidebar starts as a rail on 900-1180 px until toggled; stat-tile grids are two-up on phones; EvidenceCard draws every kind in one art box. Seeds: the demo attorney and paralegal moved to the client's office (D-052, SEED_VERSION 4). Rules recorded in docs/design/design-system.md ("Pass 2 wave A formatting pass").
rejected: Any change to palettes, type faces, radii or the three directions (Justin's styling direction comes later); hiding the duplicate table titles with CSS (removed the prop on the 13 pages instead); a "More" menu for S-22's seven actions (the two-column grid keeps every action visible and reachable); truncating BottomNav labels (guarded, not shortened); editing any module `strings.ts` (the Spanish pass owns them, so toolbar group labels reuse existing keys and the one new string, `shell.back`, lives in coreStrings).
files: src/components/template/DesktopShell/{DesktopShell.tsx,DesktopShell.css}, src/components/template/PhoneShell/PhoneShell.css, src/components/molecule/PageHeader/{PageHeader.tsx,PageHeader.css}, src/components/molecule/Section/Section.css, src/components/organism/DataTable/{DataTable.tsx,DataTable.css}, src/components/molecule/Card/Card.css, src/components/molecule/Tabs/Tabs.css, src/components/molecule/Toast/Toast.css, src/components/organism/Sidebar/Sidebar.css, src/components/molecule/BottomNav/BottomNav.css, src/components/organism/EvidenceCard/EvidenceCard.css, src/components/molecule/EmptyState/EmptyState.css, src/i18n/coreStrings.ts, src/modules/_homes/homes.css, src/modules/showcase/{showcase.css,CanvasPage.tsx,SimulatorPage.tsx}, src/dev/a11yScan.ts, src/components/molecule/StatTile/StatTile.css, src/components/molecule/SegmentedControl/SegmentedControl.css, src/components/organism/DocPreview/DocPreview.css, src/components/organism/PleadingPaper/PleadingPaper.css, src/modules/drafting/{drafting.css,DraftPage.tsx}, docs/qa/responsive-report.{md,json}, src/modules/{admin/FeedbackPage.tsx,admin/HomePage.tsx,pipeline/DeskOrdersPage.tsx,pipeline/PipelineBoardPage.tsx,counsel/HomePage.tsx,frontdesk/ClientsPage.tsx,frontdesk/HomePage.tsx,frontdesk/CallConsolePage.tsx,frontdesk/FollowUpsPage.tsx,owner/HomePage.tsx,opposition/HomePage.tsx,drafting/TemplatesPage.tsx,drafting/StudioIndexPage.tsx,plan/KanbanPage.tsx,plan/TimelinePage.tsx,plan/GraphPage.tsx}, src/auth/demoUsers.ts, src/data/MockProvider.ts, src/data/seed/{ops.ts,pipeline.ts,drafting.ts,evidence.ts,learning.ts,board.ts}, docs/design/design-system.md, docs/decisions.md, docs/changelog/_pending/format-pass.md
codes: HUB-01, F-01, F-12, F-13, F-14, F-15, L-01, L-13, L-14, L-31, L-31a, L-40, S-01, S-10, S-13, S-21, S-22, O-01, A-01, A-05, A-11, X-01, C-01, C-11, C-11a, C-20, C-21, C-22, C-40, C-42, D-21, D-22, PM-01, PM-03, PM-04, K-10, M-01

qa: `npm run qa:responsive` full matrix (80 routes x 7 widths x 2 themes, 1120 cells) ran once after the pass: 174 failing cells, all in five classes, then fixed in shared code and re-run scoped: (1) DocPreview SVG text and 8 px thumbnail overlays flagged as small text on C-11, C-11a, C-20, C-20a, C-41, D-02, L-13, L-14, L-31, S-21 (scanner now skips text inside inline SVG and aria-hidden subtrees; xs thumbnails draw a band and a dot instead of 8 px words); (2) PleadingPaper type under the floor when the sheet zooms down (floored with `--fs-floor`); (3) X-01 and L-13 horizontal scroll at 360 / 390 from a stat label and a segmented control (labels wrap, the control scrolls inside itself); (4) D-01 header actions at 360 / 390 (segmented controls span the phone grid); (5) S-22 FeedbackButton over the sticky research pane (button moves left of the pane; the studio is now `.page-bleed`). Re-run after the fixes: F-01, X-01, C-20, C-20a, D-01, D-02, S-21, L-13, L-14, L-31, HUB-01, C-11, C-11a, C-40, C-41, F-12, D-21, D-22, P-01, GB-01 all ok. Still failing for the release worker: `S-22` /assist/drafting/:draftId at 1920, 2560 and 3840 (light and dark), check `overlaps`: `.feedbackbtn` over `.drf-pane-right` (the sticky pane is taller than half the viewport at its natural position, so the scanner counts it as stuck; the button offset needs the real pane geometry, or the pane a non-sticky first screen); `C-42` /app/learn/lesson/:lessonId at every width, check `errors`: "Failed to read the 'localStorage' property" thrown by the youtube-nocookie iframe when third-party requests are blocked (pre-existing, not from this pass).

Model: **Fable 5.1** (shared code, design rules, seeds).
