# Decision log

One row per decision. Append-only: never renumber; if a decision is reversed, add a new row that supersedes it and set the old row's status to `superseded by D-0xx`. The `Source` column points at the prompt file that carries the verbatim words (or the workspace rules Justin set for every project). Statuses: `binding (Justin <date>)` (he said it), `proposed (needs Justin)` (a build choice made without him, rationale in italics), `decided`, `pending`, `superseded`, `blocked`.

| # | Date | Decision | Source | Status |
| --- | --- | --- | --- | --- |
| D-001 | 2026-09-18 | **Stack**: Vite 5 + React 18 + TypeScript strict, `react-router-dom` v6 HashRouter (GitHub Pages, no 404 fallback), plain CSS with design tokens as custom properties (no Tailwind, no CSS-in-JS), `base: './'`, `__APP_VERSION__` define, module registry by glob, `MockProvider` on localStorage first. Adapted from imagine-os/petrock (`docs/reference/hoy-petrock-patterns.md`). _Rationale: the newest hub Justin pointed at; every convention below already exists there._ | prompt 0001 ("Hoy os and petrock are good examples"), study report | proposed (needs Justin) |
| D-002 | 2026-09-18 | **Platform principles P-01..P-15 are binding** for CTL OS from the first commit and sit above the module contract in `CLAUDE.md` (`docs/platform-principles.md`). | workspace rules 2026-09-18; petrock D-209 | binding (Justin 2026-09-18) |
| D-003 | 2026-09-18 | **Page-code families**: HUB (hub), P (public site, proposal, store), C (client), F (front desk), L (attorneys), S (paralegals / assistants), O (owner), A (admin), X (opposing counsel), GB (game board), PM (project-management viewer), M (ops manual), K (docs, knowledge, legal memory), D (dev / builder tools), MK (marketing). Ranges in `docs/build-plan.md`; `defineSpec` warns on other prefixes. _Rationale: one family per role experience Justin listed, so a code tells you who it is for._ | prompt 0001 (role list) | proposed (needs Justin) |
| D-004 | 2026-09-18 | **Roles**: `super_admin, owner, attorney, paralegal, front_desk, marketing, client, opposing_counsel, public`. One fictional demo user per role; `ROLE_HOME` per role; permissions as strings checked through `can()`, never role comparisons in pages. _Rationale: the nine experiences in the brief ("front desk, owner, lawyer, assistant, customer, opposition..." plus marketing and the public)._ | prompt 0001 | proposed (needs Justin) |
| D-005 | 2026-09-18 | **Hub with view-as and dev mode**: from HUB-01 choose who you are logged in as (any role / demo user) and toggle developer mode (specs, inspector, placeholders visible on every page). Guards stay real; only identity is mocked until real auth. | prompt 0001 ("from the hub, choose who we're logging in as", "Turn on and off developer mode") | binding (Justin 2026-09-18) |
| D-006 | 2026-09-18 | **Every row carries `id, tenant_id, created_at, updated_at, version`** from day one. `tenant_id` because the firm is a network of regional attorneys (multi-office, potentially multi-firm); `version` for optimistic concurrency (P-14). _Rationale: adding them later means touching every table; hoy already uses `tenant_id`._ | prompt 0001 (multiplayer expectation), workspace rule "ids, updated_at ... from day one" | proposed (needs Justin) |
| D-007 | 2026-09-18 | **Actions manifest from day one**: `PageSpec.actions: ActionDef[]` (`id '<module>.<verb>', label, intent, permission?, params?`), actions bus, `/#/dev/actions` (D-20). The registry is the WebMCP surface and the voice vocabulary; every UI change updates it. | workspace rule "Control"; petrock D-197 | binding (Justin 2026-09-18) |
| D-008 | 2026-09-18 | **`Placeholder` atom from day one**: tooltip + "not wired yet" toast + dashed outline in dev mode + `data-placeholder`; `PageStub` uses it; no control silently does nothing. | workspace rule "Placeholders"; petrock D-202 | binding (Justin 2026-09-18) |
| D-009 | 2026-09-18 | **Annotations columns from day one**: `feedback` table has `kind (comment|request|bug), element_path, component, viewport, theme, screenshot_url, status, triage, triage_note, decision_ref` plus author and role; agents record the triage decision before changing anything (`docs/reference/annotations-triage.md`). | workspace rule "Annotations"; petrock D-200 | binding (Justin 2026-09-18) |
| D-010 | 2026-09-18 | **English and Spanish toggle from the start**; every string through `useT()`; Spanish fill is a pass, never a blocker. | workspace rule "Languages" | binding (Justin 2026-09-18) |
| D-011 | 2026-09-18 | **English is the default language**; the client app and video captions get the first Spanish fill. _Rationale: California firm, English-language site; many tenant clients are Spanish speakers, so the client surfaces come first._ | prompt 0001 (site is English) | proposed (needs Justin) |
| D-012 | 2026-09-18 | **Model routing**: Fable for judgment, architecture, shared code, seams, integration; Opus 5 for building modules and pages; Sonnet 5 for mechanical passes (screenshots, Spanish fill, QA matrices). Every reply and every task states its model. | workspace rule "Model routing"; prompt 0001 ("clarity on what model you're using") | binding (Justin 2026-09-18) |
| D-013 | 2026-09-18 | **Plan units are dependency "ticks", not days**: a task starts when its dependencies are done; the timeline (PM-03) has a tick axis; no calendar estimates anywhere. | prompt 0001 ("we're not bound by normal days of development") | binding (Justin 2026-09-18) |
| D-014 | 2026-09-18 | **PM viewer reads `docs/plan/tasks.json`** (kanban, list, timeline with dependencies, dependency graph as an object view, task detail, passes); ids `T-nnn` are stable forever; `kanban.md` mirrors statuses; later the file seeds a `plan_tasks` table. | prompt 0001 ("Kanban, a list view, ... timeline view with dependencies") | binding (Justin 2026-09-18) |
| D-015 | 2026-09-18 | **Git only, Pages via GitHub Actions**: push to `main`, no PRs, no Slack posts from agents, no Contents API; `npm run build` green before every push; workflow as petrock's `pages.yml`. | prompt 0001 ("source set to github actions"), workspace rule "Publishing" | binding (Justin 2026-09-18) |
| D-016 | 2026-09-18 | **Company-OS is reference only**: nothing wires into it until Justin says so; `CompanyOsProvider` is the seam; its DB conventions inform ours (`docs/reference/company-os.md`). | prompt 0001 ("Reference but dont actually use the company os repo") | binding (Justin 2026-09-18) |
| D-017 | 2026-09-18 | **Supabase (DB + Auth) and Stripe (payments + payroll) are the assumed later backends** (Pass 4), behind `DataProvider` / `PaymentProvider` seams; nothing else decided ("the rest we can figure out later"). | prompt 0001 | binding (Justin 2026-09-18) |
| D-018 | 2026-09-18 | **Pleading-paper editor**: browser rich-text editor with a numbered-line ruler (California pleading paper), caption block and footer as components, PDF and DOCX export, documents and revisions as rows, realtime multi-editor through a provider in Pass 3 (CRDT / operations). Spike in Pass 2 (T-069). _Rationale: replaces WordPerfect with something Google-Docs-like without depending on Google; export keeps court-filing compatibility._ | prompt 0001 ("better pleading paper system, multi-editor, like Google Docs") | proposed (needs Justin) |
| D-019 | 2026-09-18 | **Legal memory = `docs/legal/`**: a statute index with `verified_on` per row, an append-only law-change log, one topic file per practice area; every deadline / cost rule in code cites a row; a rule change requires a log entry; nothing is treated as current until an attorney sets `verified_on`. Rendered at K-10. | prompt 0001 ("clear on what changes to the laws have been made") | binding (Justin 2026-09-18) |
| D-020 | 2026-09-18 | **Game board as data** (`docs/game-board/nodes.json`: phases, nodes, edges typed by the board's five path types), rendered as an interactive 2D SVG first (GB-01, Pass 1), then a 3D object view (GB-04, Pass 2, graph-gallery Objects 3D pattern), with case mode and cost / if-then overlay. _Rationale: the 2D board is the firm's known asset and ships in the first release; 3D builds on the same data._ | prompt 0001 ("turn into something more three-dimensional and more complete") | proposed (needs Justin) |
| D-021 | 2026-09-18 | **Canvas (D-21)**: every page laid out on one zoomable surface, each frame live and usable, not just a picture. | prompt 0001 | binding (Justin 2026-09-18) |
| D-022 | 2026-09-18 | **Demo simulator (D-22)**: phone and desktop frames with device presets for presenting everything built. | prompt 0001 | binding (Justin 2026-09-18) |
| D-023 | 2026-09-18 | **No real attorney or client names in seed data**: demo users and seed rows are fictional; the founder and network attorneys appear only in the proposal / offices content once Justin confirms. _Rationale: petrock / hoy rule "never commit real personal data"; the firm's people have not consented._ | study report (hoy CLAUDE.md) | proposed (needs Justin) |
| D-024 | 2026-09-18 | **Justin must enable Pages once** (Settings > Pages > Source = GitHub Actions); the workflow token cannot create the site. Until then the deploy job fails on the first run. | petrock changelog 0020 (known gotcha) | pending |
| D-025 | 2026-09-18 | **Firm facts are "as indexed", not verified**: caltenantlaw.com was unreachable from the build environment; prices, video order, the eighth office, brand visuals, scheduler and forms vendors are flagged unverified in `docs/project-brief.md` and `docs/reference/firm-site-digest.md` and never shown as facts in the product without the flag. | firm brief (method note) | superseded (D-038) |
| D-026 | 2026-09-18 | **Appliance vision**: CTL OS must be installable on a Linux distro and boot directly into the software, capable of every function the firm needs (video, telephone, email, SMS, WhatsApp, CRM, project management, contract signing, drafting, legal research). Planned as Pass 5 with seams built earlier. | prompt 0001 | binding (Justin 2026-09-18) |
| D-027 | 2026-09-18 | **One comms seam**: every channel (video, phone, email, SMS, WhatsApp) writes the same `message_log` rows through a `CommsProvider`; evidence import reads them. _Rationale: hoy's `comms.ts` pattern; one inbox, one evidence source._ | study report (hoy comms seam) | proposed (needs Justin) |
| D-028 | 2026-09-18 | **Discovery and the client binder are core Pass 2 modules**: requests to the client, uploads, email / text evidence import, binder by board stage, discovery tracker (RFAs, interrogatories, RFPs, meet and confer, compel). | prompt 0001 ("Discovery in particular is a big issue") | binding (Justin 2026-09-18) |
| D-029 | 2026-09-18 | **Learning management**: the firm's videos and articles are a curriculum with watched state per client, content dripped along the case journey, attorneys see what a client has learned before a consultation. | prompt 0001 | binding (Justin 2026-09-18) |
| D-030 | 2026-09-18 | **Cost roadmap**: costs are modelled per board node / edge with if-then scenarios, shown as a calendar of potential costs and a "what to pay next" step; every band cites a SKU (prices unverified until D-025 clears). | prompt 0001 | binding (Justin 2026-09-18) |
| D-031 | 2026-09-18 | **Docs are English with a Spanish summary; the ops manual is English source with an `es/` mirror of the same file names** (the firm's staff work in English; hoy's Spanish-first rule is inverted here). _Rationale: D-011._ | study report (hoy documentation rules) | proposed (needs Justin) |

| D-032 | 2026-09-18 | **Design system adopted: paper / ink / sky / amber.** Paper light mode and night dark mode neutral ramps, ink surfaces, the clearing-sky hero gradient, sky `#3E9BE0` as the accent and amber `#E1891C` as the one CTA colour; hairlines and tinted shadows instead of grey borders; fluid display type (44-96 px) on Source Serif 4; radii 10 / 16 / 24. Every value lives in `src/design/tokens.ts`; the rationale and the rules are in `docs/design/design-system.md` (changelog 0003). | prompt 0002 ("this is ugly. make it gorgeous") | binding (Justin 2026-09-18) |
| D-033 | 2026-09-18 | **Worktree-per-module build workflow.** Each Pass 1 group built on its own branch (`mod/<module>`) in a git worktree off `main`, touching only its own folders plus glob-aggregated per-module files; the integrator (Fable) merges in a fixed order (design first, then plan, site, board, homes, showcase, knowledge), resolves the few shared-file conflicts by hand, regenerates `tokens.css`, `schema.sql`, `data-model.md` and `specs.md` once, and folds `_pending` changelogs into numbered entries (changelog 0010). Module folder names are decided by the worker's brief, not the plan's sketch; the plan's deliverable paths are updated to the shipped names and task ids never change. | integration T-050 | proposed (needs Justin) |
| D-034 | 2026-09-18 | **Frame-session URL params are a machine-drivable surface.** `#/<route>?as=<role>&dev=0|1&lang=en|es&theme=light|dark` renders any page as any demo role inside an iframe (`src/modules/showcase/frameSession.ts` shadows `ctl.session` / `ctl.lang` / `ctl.theme` in that frame's realm only, so a frame can never change the parent's session), and `#/dev/simulator?device=&route=&role=&lang=&theme=&dev=&rot=&present=&step=` addresses a whole demo. Recorded in `docs/reference/surfaces.md`; a voice controller, an agent or a test drives them by URL alone. | changelog 0008 | proposed (needs Justin) |
| D-035 | 2026-09-18 | **Provisional ops schema, superseded by T-054.** `src/data/schema/ops.ts` ships eleven tables (`cases, deadlines, assignments, consultations, intakes, documents, invoices, lessons, lesson_progress, service_events, meet_confer`) and `boardStages.ts` so the seven role homes read real rows in Pass 1. T-054 (Fable) replaces them with the case domain; the names are meant to survive, `stage_node_id` stays the join to the board, and `board_positions.case_id` becomes a reference to `cases.id` (the board seed already carries the ops demo client's `case_01`). | changelog 0007 | proposed (needs Justin) |
| D-036 | 2026-09-18 | **`order_index`, never `order`, for ordering columns.** `order` is a reserved word in Postgres; every table that orders rows uses `order_index` (`plan_tasks` first). `npm run sql` generates from the schema files, so the rule is enforced by review, not by the generator (a check is a follow-up). | changelog 0004 | proposed (needs Justin) |
| D-037 | 2026-09-18 | **One site module.** `src/modules/site` holds the public landing (P-01) and the proposal pages (P-02..P-04): they share the `SiteLayout` frame, one nav, one string table and one `tasks.json` lookup helper (`planFor`), and the registry keys on path, not folder. The build plan's `proposal` module is retired; T-112 (proposal polish) points at `src/modules/site`. | changelog 0005 | proposed (needs Justin) |
| D-038 | 2026-09-18 | **Firm facts are scraped live, still unverified by the firm.** caltenantlaw.com was scraped on 2026-09-18 (prompt 0004): every fact about the firm (prices, SKUs, videos, articles, offices, brand) comes from `docs/data/*.json` and `docs/reference/*` with `evidence: "scraped-live"`, `scraped_at` and `verified: false`; the product may show them as "as posted on <date>" but not as confirmed until an attorney or Justin sets `verified: true` (same rule as `docs/legal` rows). Category ids in the catalog are the site's own `/store/<slug>` paths so the firm's menu and ours stay linkable. Replaces D-025's "as indexed" regime. | prompt 0004 (Justin: "rescrape please"; "their menu of categorized services and deliverables is a big amazing part of their system") | decided |
| D-039 | 2026-09-18 | **Three switchable visual directions, Justin picks one.** `clearsky` (the D-032 system evolved; default, `ctl` aliased), `boardgame` (felt / cardstock / the poster's KEY, Bricolage Grotesque) and `courthouse` (ink on off-white, one vermilion accent, hairlines, serif at 500, mono labels) are `data-brand` token sets on identical components and layout, switchable from the hub header and `?brand=`; all AA in light and dark. After the pick the other two leave the picker. _Rationale: prompt 0003 gave no signal on what is wrong with the looks; three real alternatives beat a fourth guess._ | prompt 0003 ("still not happy with the looks") | proposed (needs Justin: pick one) |

| D-040 | 2026-09-18 | **The catalog is the live scrape, not the reconstruction.** `docs/data/services-catalog.json` from prompt 0004 (caltenantlaw.com read on 2026-09-18: `/all-services` + the Ecwid storefront API; 98 rows, 48 categories) replaces the 0.1.0 search-index reconstruction the catalog module was built against; the seed reads its fields directly (`phase` ids, `store_order`, `time_expectation`, `client_inputs`, `deliverable_format`, `not_included`, `evidence: scraped-live`, `verified: false`) and every price badge says "as listed on caltenantlaw.com on 2026-09-18 · unverified". _Rationale: real prices exist now; two catalogs would drift._ | prompt 0003 + 0004; integration 0.1.1 | decided |
| D-041 | 2026-09-18 | **P-13 shows the store as the firm presents it, with the hidden stage map one switch away.** Default outline top level = the 20 visible `/store` menu categories in menu order with products in store order (two levels, no visible sub-categories on the live store); "Firm's stage map" (`?view=stages`) switches to the hidden legacy Ecwid tree (Consultation · I'm Being Evicted... · Legal Papers · Sue Your Landlord, four levels, `service_categories.hidden`, `parent_id`), a product under every hidden leaf it is filed in, plus "Store menu only" for the products the old tree never filed. The three CTL OS grouping parents from the reconstruction are dropped. _Rationale: both hierarchies are the firm's own; the visible one is what a visitor sees, the hidden one is their service-to-stage map (Justin: "their menu of categorized services and deliverables is a big amazing part of their system")._ | prompt 0003 (follow-up: outline view); coordinator note 0.1.1 | decided |
| D-042 | 2026-09-18 | **The firm's illustrations are an assets table, used only where `suggested_use` says.** `docs/data/illustrations.json` (202 images scraped live) seeds `illustrations` (195 rows; the seven attorney portraits are neither seeded nor bundled, D-023); files in `reference/site-scrape/assets/` are bundled by a Vite glob (no copy into `public/`). Used in the product: product circle icons on P-10 / P-11 / P-13 rows and cards (`service:<sku>`), category tiles and descriptions as P-10 category headers (`store-category:<slug>`), video thumbnails on C-03 (`video:<slug>`), hero poster / Game Board poster / three cartoon tiles on P-01 (`brand`, `nav-tile`). D-23 `/dev/illustrations` browses the table by style family or use. Rights stay with the firm (© Kenneth H. Carlson / Carlson Law Office); every row is `verified: false` until the firm confirms the use, and the pages say so. | prompt 0004 second pass (Justin's additions relayed); integration 0.1.1 | proposed (needs Justin: confirm the firm's assets may appear in the proposal and product) |
| D-043 | 2026-09-18 | **`videos.json` is the LMS curriculum source.** `lessons` seeds from the live scrape (33 library videos in the page's own order and three groups, plus three embedded-only videos; lengths from YouTube; thumbnails from the illustrations table) instead of the hand-typed 18-row list; `lesson_progress` and the demo client's progress carry over on the new ids (`les_<video id>`). Articles (`articles.json`) wait for T-077's article pass. | prompt 0004; integration 0.1.1 | decided |
| D-044 | 2026-09-18 | **Offices in the seed are the firm's real offices, attorney names are not.** The `tenants` seed carries the eight offices as posted on caltenantlaw.com/offices on 2026-09-18 (`docs/data/offices.json`: office name, city, coverage, address, the firm's phone; `ten_slo` San Luis Obispo County added, ids of the other seven unchanged) so P-01 and the site footer show real offices and cities; attorney names, bar numbers and portraits stay out of the UI and the seed until Justin confirms (D-023 unchanged). | prompt 0004; integration 0.1.1 | decided (names: still proposed, needs Justin) |

| D-045 | 2026-09-20 | **Pass 2 is two waves; release 0.2.0 = wave A.** Justin's 2026-09-20 asks (prompt 0006: formatting pass, LMS, attorney pictures, game board check, role scoping, homepage order, simulator devices, canvas of resizable windows with role flows, document previews, a per-document pipeline with explicit "waiting on client", client and front desk views of it, a call console, a drafting studio with laws at the side, evidence upload and email / text import) become wave A of Pass 2 (T-118..T-136 plus T-054 scoped, T-063, T-065, T-069, T-070..T-072, T-078) and ship as release 0.2.0 (T-088 retargeted to T-135); the untouched Pass 2 tasks are wave B and ship as 0.2.1 (T-136). No task is renumbered. | prompt 0006 ("Please update the plan and build accordingly") | binding (Justin 2026-09-20) |
| D-046 | 2026-09-20 | **Attorney portraits and the names as shown on caltenantlaw.com appear in the product and the proposal, badged "as shown on caltenantlaw.com, unverified"** until the firm confirms each name, bar number and office (T-133 catalogues them in `docs/data/people.json`, `evidence: scraped-live`, `verified: false`). Supersedes the "never shown" half of D-023 and D-044; demo users stay fictional and no client is ever real. | prompt 0006 ("bring in the pictures of the attorneys and any other pictures you didnt scrape yet") | binding (Justin 2026-09-20) |
| D-047 | 2026-09-20 | **Order pipeline stages (canonical list, `src/domain/pipeline.ts`):** `new_order`, `payment_confirmed`, `assigned`, `gathering_client_details` (waits on client), `details_complete`, `first_draft`, `attorney_review`, `client_review` (waits on client; revision counter), `client_requested_changes`, `approved_by_client`, `supervisor_review`, `supervisor_changes`, `final_signed`, `filed_or_scheduled` (waits on court), `served`, `proof_of_service`, `hearing_scheduled` (waits on court), `done`; plus `on_hold` and `cancelled`. Every stage says whom it waits on (client / court / staff / nobody) so "waiting on the client" is a column, not a guess; the client sees plain-language labels, front desk reads the same row. Steps beyond Justin's list (payment confirmed, assigned, attorney review before the client sees a draft, final signed, proof of service, hearing scheduled, on hold / cancelled) are ours. | prompt 0006 ("the attorneys need a pipeline for each document ... knowing when things are being waited on by the clients") | proposed (needs Justin: confirm the added steps) |
| D-048 | 2026-09-20 | **Role scoping: a route's `roles` array is the source of truth; shells, hub cards and the role matrix (D-24) derive from it through `hasRole`.** Developer tools (D-xx), docs (K-01..K-03) and the plan (PM-xx) are staff-leadership surfaces: `super_admin`, `owner` (D-21 canvas and D-22 simulator included; the other D-xx pages super_admin only). Legal memory and the law-change log (K-10..K-13) add `attorney` and `paralegal`. Front desk (F-xx) is `front_desk`, `owner`, `super_admin`. Front desk and marketing never see developer, docs, plan, legal memory or law-change pages. A super admin viewing as a role is scoped exactly like that role. The proposal pages embed plan data at build time, so the public never needs the PM routes. | prompt 0006 ("front desk should not be seeing the developer screens and does not need things like legal memory or law change. Check for all roles") | decided |
| D-049 | 2026-09-20 | **The canvas (D-21) is a free-form zoomable canvas of resizable browser windows** with saved layouts (`page_layouts` rows, per viewer and shareable) and a flows layer that draws what each role can do as connectors between windows (`src/flows/roleFlows.ts`). Supersedes the shelf-packed viewer built under T-025 (D-021 stays: every frame live and usable). | prompt 0006 ("a zoomable canvas with web browser windows, with adjustable height / width ... flow chart lines to show the flow of what each type of user can do") | binding (Justin 2026-09-20) |
| D-050 | 2026-09-20 | **The demo simulator (D-22) picks the device from the route's surface** unless overridden: `customer` -> phone, owner dashboards (`owner`) -> TV, every other staff / knowledge surface -> desktop, public site -> laptop; device chrome differs per preset so a demo is recognisably on the device it represents. | prompt 0006 ("make sure the demo simulator shows the demos in the proper device being represented") | binding (Justin 2026-09-20) |
| D-051 | 2026-09-20 | **`DocPreview` renders document previews from metadata (SVG / CSS), never by rasterising a file**: one organism with a silhouette per kind (pleading, letter, court form, evidence photo, email, text thread, receipt, video), used wherever a document, evidence item or lesson is listed (binder, pipeline board and order detail, drafting studio, LMS, opposing-counsel portal). Real thumbnails of stored files can replace the silhouette later without changing callers. | prompt 0006 ("create a preview image of any type of document") | decided |
| D-052 | 2026-09-20 | **The demo attorney and paralegal work in the demo client's office.** `usr_attorney` (Mateo Ruiz) and `usr_paralegal` (Nia Bennett) are Riverside (`ten_inland`) staff, on `case_01`, `ord_0131`, `drf_0131` and the seeded evidence with the demo client; the fictional pair Priya Raghunathan / Cheyenne Vossberg took Downtown LA as `usr_atty_dtla` / `usr_para_dtla` so the network still has a second busy office. Reason: with the demo pair in DTLA, L-14, L-31 and S-22 opened on another office's empty state for the attorney and the paralegal, which read as broken. Multi-office scoping stays demonstrated by the owner's network view and the DTLA cases. `SEED_VERSION` 4. | Prompt 0006 formatting pass (Fable) | proposed (needs Justin) |

## Resumen en español

Registro de decisiones, solo se añade: cada fila tiene fecha, decisión, fuente (el prompt con las palabras exactas o las reglas del workspace) y estado (`binding` si Justin lo dijo, `proposed (needs Justin)` si es una elección de construcción). Decisiones clave: stack Vite/React/TS con HashRouter (propuesto), principios de plataforma obligatorios, familias de códigos de página, nueve roles, `tenant_id` y `version` en cada fila, registro de acciones y `Placeholder` desde el primer día, inglés/español desde el inicio, unidades de plan en "ticks", Company-OS solo como costura, Supabase y Stripe después, editor de papel de alegatos en el navegador (propuesto), tablero como datos 2D primero y 3D después, memoria legal con registro de cambios de ley, sin nombres reales en los datos de ejemplo, Justin debe habilitar Pages.
